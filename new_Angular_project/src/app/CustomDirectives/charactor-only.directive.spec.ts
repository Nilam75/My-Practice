import { CharactorOnlyDirective } from './charactor-only.directive';

describe('CharactorOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new CharactorOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
